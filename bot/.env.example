# ── Supabase (anon key is public by design; RLS must be locked down) ──
SUPABASE_URL=https://xmxywlyqdqrfrojwggkt.supabase.co
SUPABASE_ANON_KEY=

# ── Facebook accounts (cookies in Supabase `sessions` are used first) ──
# JSON array, rotated one per cycle
FB_ACCOUNTS=[{"email":"ilse2taylor@gmail.com","password":"CHANGE_ME"},{"email":"projects.reports.ilse@gmail.com","password":"CHANGE_ME"}]

# ── AI classification for borderline posts ──
GEMINI_API_KEY=

# ── Email alerts + 6pm daily report (Gmail app password) ──
ALERT_EMAIL=
ALERT_EMAIL_PASSWORD=

# ── Optional SOCKS5 proxy ──
PROXY_SERVER=
PROXY_USERNAME=
PROXY_PASSWORD=

# ── Runtime ──
DRY_RUN=false
SCAN_INTERVAL_SECONDS=1800
COMMENT_DELAY_MIN_SECONDS=45
COMMENT_DELAY_MAX_SECONDS=150
GROUPS_PER_CYCLE=10
HEADLESS=false
PORT=8080
